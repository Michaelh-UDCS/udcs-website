rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all access unless explicitly authenticated/authorized
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
